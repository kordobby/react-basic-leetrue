import React from "react";
import { BrowserRouter, Route, Routes } from "@remix-run/router";

// Route 설정 뼈대 잡기
const Router = () => {
  return (
    <BrowserRouter>
      <Routes>
        {/*
        
        */}
      </Routes>
    </BrowserRouter>
  );
};

export default Router;
